{
  "name": "png-minifier",
  "productName": "PNG Minifier",
  "version": "0.1.0",
  "description": "A PNG minifier based on UPNG.js",
  "main": "./js/main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=asset/pngm.icns --prune=true --out=release-builds"
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^1.7.9",
    "electron-package": "^0.1.0",
    "webpack": "^3.10.0"
  },
  "dependencies": {
    "pako": "^1.0.6"
  }
}
